# Port targets — `toml-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `toml` that codex-rs uses, and therefore the minimum subset that `toml-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-161] (rank `122`, tier `direct`, status `To Do`)

**Cargo package name**: `toml`
**Rust module name (in `use` statements)**: `toml`

## Summary

- **6** distinct symbols imported by codex-rs
- **53** total `use` statements
- **46** codex source files affected
- **12** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `toml::Value`

- **44 `use` site(s)** across **12 codex member crate(s)**: `codex-app-server`, `codex-app-server-client`, `codex-cli`, `codex-config`, `codex-core`, …
- Aliases used: `as TomlValue`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-client/src/lib.rs:60` (`codex-app-server-client`) — `use toml::Value as TomlValue`
- `app-server/src/config/external_agent_config.rs:33` (`codex-app-server`) — `use toml::Value as TomlValue`
- `app-server/src/config_manager.rs:23` (`codex-app-server`) — `use toml::Value as TomlValue`
- `app-server/src/config_manager_service.rs:36` (`codex-app-server`) — `use toml::Value as TomlValue`
- `app-server/src/in_process.rs:94` (`codex-app-server`) — `use toml::Value as TomlValue`
- `app-server/src/request_processors.rs:408` (`codex-app-server`) — `use toml::Value as TomlValue`
- `cli/src/debug_sandbox.rs:30` (`codex-cli`) — `use toml::Value as TomlValue`
- `config/src/fingerprint.rs:6` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/key_aliases.rs:1` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/lib.rs:128` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/loader/layer_io.rs:13` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/loader/macos.rs:13` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/loader/mod.rs:42` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/mcp_edit.rs:8` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/merge.rs:3` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/overrides.rs:1` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/project_root_markers.rs:2` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/state.rs:15` (`codex-config`) — `use toml::Value as TomlValue`
- `config/src/thread_config.rs:9` (`codex-config`) — `use toml::Value as TomlValue`
- `core-plugins/src/manager_tests.rs:34` (`codex-core-plugins`) — `use toml::Value`
- _… and 24 more (see `codex_external_usage.json` for the full list)._

### `toml::toml`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/suite/client.rs:75` (`codex-core`) — `use toml::toml`
- `core/tests/suite/unstable_features_warning.rs:16` (`codex-core`) — `use toml::toml`

### `toml::from_str`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-config`, `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/config_requirements.rs:1206` (`codex-config`) — `use toml::from_str`
- `core/src/config/config_loader_tests.rs:2263` (`codex-core`) — `use toml::from_str`

### `toml::value::Table`

- **2 `use` site(s)** across **2 codex member crate(s)**: `codex-config`, `codex-utils-cli`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/overrides.rs:17` (`codex-config`) — `use toml::value::Table`
- `utils/cli/src/config_override.rs:102` (`codex-utils-cli`) — `use toml::value::Table`

### `toml::Table`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-features`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `features/src/lib.rs:15` (`codex-features`) — `use toml::Table`
- `features/src/tests.rs:14` (`codex-features`) — `use toml::Table`

### `toml::map::Map`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-config`
- Aliases used: `as TomlMap`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `config/src/key_aliases.rs:2` (`codex-config`) — `use toml::map::Map as TomlMap`
