# Rust callers — `toml-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `toml` | `toml` | 53 | 12 | 1 | 53 |

## `toml`

### Inbound symbols (6 distinct, 53 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `toml`.

#### `toml::Value` — 44 site(s) (44 codex, 0 other, 12 caller crates)

- ✅ `codex-kotlin/codex-app-server` `src/config/external_agent_config.rs:33` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-app-server` `src/config_manager.rs:23` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-app-server` `src/config_manager_service.rs:36` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-app-server` `src/in_process.rs:94` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-app-server` `src/request_processors.rs:408` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-app-server-client` `src/lib.rs:60` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-cli` `src/debug_sandbox.rs:30` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-config` `src/fingerprint.rs:6` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-config` `src/key_aliases.rs:1` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-config` `src/lib.rs:128` — `pub_use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-config` `src/loader/layer_io.rs:13` — `use toml::Value as TomlValue`
- ✅ `codex-kotlin/codex-config` `src/loader/macos.rs:13` — `use toml::Value as TomlValue`
- _… and 32 more site(s)._

#### `toml::Table` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-features` `src/lib.rs:15` — `use toml::Table`
- ✅ `codex-kotlin/codex-features` `src/tests.rs:14` — `use toml::Table`

#### `toml::from_str` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-config` `src/config_requirements.rs:1206` — `use toml::from_str`
- ✅ `codex-kotlin/codex-core` `src/config/config_loader_tests.rs:2263` — `use toml::from_str`

#### `toml::toml` — 2 site(s) (2 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `tests/suite/client.rs:75` — `use toml::toml`
- ✅ `codex-kotlin/codex-core` `tests/suite/unstable_features_warning.rs:16` — `use toml::toml`

#### `toml::value::Table` — 2 site(s) (2 codex, 0 other, 2 caller crates)

- ✅ `codex-kotlin/codex-config` `src/overrides.rs:17` — `use toml::value::Table`
- ✅ `codex-kotlin/codex-utils-cli` `src/config_override.rs:102` — `use toml::value::Table`

#### `toml::map::Map` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-config` `src/key_aliases.rs:2` — `use toml::map::Map as TomlMap`
